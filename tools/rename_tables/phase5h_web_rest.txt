# Phase 5h — die Reste der Seite: JS-Lokale, data-Attribute, IDs, CSS-Klassen
# und -Variablen, die Phase 3 uebersehen hatte. Zwei Laeufe:
#   python tools/rename.py --table tools/rename_tables/phase5h_web_rest.txt --js-only
# fuer die Bezeichner, und dieselbe Tabelle noch einmal ohne --js-only, damit
# die Bindestrich-Namen (Klassen, Variablen, data-Attribute) auch in den
# Python-Strings der Tests und in CLAUDE.md nachgezogen werden.

# --- JS-Bezeichner -----------------------------------------------------------------
alle=all
alsAnderes=asOther
erkTool=detTool
halt=atBreakpoint
klapp=collapse
nach_phase=to_phase
neu=next
optionenNeu=refreshOptions
pruefen=poll
scanSchritt=scanStep
werkzeuge=creator
zusatz=extra

# --- data-Attribute, IDs, Klassen, Variablen (Bindestrich = ueberall) -----------------
als-anderes=as-other
erk-tool=det-tool
data-klapp=data-collapse
scan-schritt=scan-step
data-sequenz=data-sequence
scan-pin-kurz=scan-pin-short
scan-pin-lang=scan-pin-long
slot-fremd=slot-foreign
slot-offen=slot-empty
.gitter2=.grid2
.gitter3=.grid3
.lauf=.run
.teilen=.share
