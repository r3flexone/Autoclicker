# Phase 5e — die Lokalen der Werkzeuge unter tools/ (rename.py, log_report.py,
# symbol.py, llm_bench.py, slot_tester.py). Nur .py; nach dem Lauf wird alles
# ausser tools/ zurueckgesetzt.
# Angewendet mit: python tools/rename.py --table tools/rename_tables/phase5e_tools.txt --python-only

neu=new
alt=old
inhalt=content
hinweise=hints
hinweis=hint
tiefe=depth
antwort=answer
gesamt_dauer=total_duration
namen=names
zeile=line
ende=end
feld=field
einmal=once
timeouts_je_schritt=timeouts_per_step
treffer=match
ausdruck=expression
kopf=head
versatz=offset
_ersatz=_replacement
ersatz=replacement
gross=big
frage=ask_fn
lauf=run
flaeche=surface
ausschnitt=crop
gruende=reasons
zeiten=times
fehler=error
eigen=own
eigene_namen=own_names
quellen=sources
paare=pairs
paar=pair
mit=with_underscore
ohne=without_underscore
stopps=stops
anzahl=count
alt_zeilen=old_lines
neu_zeilen=new_lines
schwanz=tail
zeile_davor=line_before
bezeichnung=designation
stueck=piece
bilder=images
proben=samples
