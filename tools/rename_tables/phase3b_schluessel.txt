# Phase 3b — die JSON-Schluessel zwischen Bruecke und Seite, einschliesslich
# der Statusdateien (.lauf.json, .nachklick.json, .aufnahme.json), die durch die
# Bruecke bis in die Seite laufen — transient, also kein Bestand.
# Modus --keys: in Python nur Strings, die GENAU der Schluessel sind; in JS die
# Identifier (z.art, {art: 1}) und exakte Strings. Nicht --strings: das traefe
# `art` auch in "scan-art".
# Ausgenommen (persistierte Dateien, nach dem Lauf zurueckgesetzt):
# autoclicker/katalog.py und tools/katalog.py (katalog.json: kategorie, wert).
# Angewendet mit: python tools/rename.py --table tools/rename_tables/phase3b_schluessel.txt --keys

abschnitte=sections
aktion=action
aktionen=actions
aktiv=active
aktuell=current
anzahl=count
art=kind
aufgenommene_farbe=captured_color
ausgewaehlt=ticked
auswahl=selection
bereich=area
bestaetigen=confirm
bestaetigt=confirmed
bestaetigung=confirmation
bestaetigung_verzoegerung=confirmation_delay
bild=image
bis=until
block_titel=block_title
bloecke=blocks
breite=width
datei=file
dauer=duration
einheit=unit
else_aktion=else_action
else_aktionen=else_actions
else_punkt=else_point
ende=end
endpunkt=endpoint
ereignisse=events
erkannt=detected
erkannt_in=detected_in
erkannt_slots=detected_slots
erwartet=expected
farbe=color
farben=colors
farbfeld=color_swatch
farbtext=color_text
fehlende_scan_groessen=missing_scan_sizes
fenster=window
fenster_id=window_id
fenster_referenz=window_reference
fenster_titel=window_title
fenster_verfuegbar=window_available
foto=photo
fremd=foreign
fremd_slots=foreign_slots
geaendert=changed
geprueft=checked
gesamt=total
gesendet=sent
gesetzt=placed
gewaehlt=selected
grund=reason
haltepunkt=breakpoint
hilfe=help
hinweise=hints
hoehe=height
identitaet=identity
ist=actual
items_gesamt=items_total
kann_hinzufuegen=can_add
kategorie=category
kategorien=categories
laeuft=running
leer=empty
links=left
llm_endpunkt=llm_endpoint
loeschbar=deletable
manuell=manual
marker_gesamt=marker_total
marker_noetig=marker_required
meldung=message
nach_zeile=to_row
nummer=number
oben=top
ohne_wert=without_value
optionen=options
phasen=phases
prioritaet=priority
prueft=checks
punkt=point
punkt_andere=point_others
punkt_farbe=point_color
punkte=points
quelle=source
quelle_fenster=source_window
schluessel=key
seit=since
sequenz=sequence
sequenzen=sequences
skala=scale
soll=target
sonstige=others
staende=states
stand=stamp
stelle=position
stoerungen=disturbances
suchbereich=search_area
timeouts_gesamt=timeouts_total
titel=title
toleranz=tolerance
trigger_pruefen=trigger_check
trigger_punkt=trigger_point
uebersprungen=skipped
verfuegbar=available
verify_miss_gesamt=verify_miss_total
verify_ok_gesamt=verify_ok_total
verify_punkt=verify_point
verlauf=history
verwendungen=usages
verzoegerung=delay
vollbild=fullscreen
von_zeile=from_row
vorhanden=existing
vorlagengroessen=template_sizes
vorschau=preview
warte_timeout=wait_timeout
warten=waiting
wert=value
werte=values
winzig=tiny
zaehler=counters
zaehlt=counts
zeile=row
zeilen=rows
ziel_fenster=target_window
zyklen=cycles
zyklus=cycle

# data-*-Attribute, die die Seite ueber dataset.<schluessel> liest
data-vorhanden=data-existing
